# Trace Correlation

Status
- Disabled in devtools while the common model is being implemented.
- Previous correlation relied on `gateway_remote_id` in `mail.message`, which is
  not part of the new model.
